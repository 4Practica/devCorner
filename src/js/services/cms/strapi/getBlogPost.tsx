// const STRAPI_URL = import.meta.env.VITE_CMS_URL
export const getBLogPostByPath = () => {
  /**
   *  function
   * getsBlogPost using fetchData service keeping the contract
   */
  try {
    // const request = fetch(STRAPI_URL + '/')
  } catch (error) {
    // Non empty catch
  }
  return
}
